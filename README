Caller-saved 寄存器（调用者保存）
    包括参数传递寄存器（如 RISC-V 中的a0-a7）和临时寄存器（如x5-x7、x28-x31）。
    规则：若调用者（caller）在函数调用后仍需要这些寄存器的值，则必须在调用前自行保存（如压栈）。
    用途：用于传递函数参数、存储临时计算结果等短期数据。
Callee-saved 寄存器（被调用者保存）
    包括保存寄存器（如 RISC-V 中的s0-s11）和帧指针（如fp/x8）。
    规则：被调用函数（callee）在使用这些寄存器前必须先保存其原值，并在返回前恢复。
    用途：用于存储需要在函数调用链中持续保留的数据（如局部变量、函数调用上下文）。

调用proc.c 457 scheduler函数为什么需要通过swtch.S中的swtch函数？
    个人理解：如果通过函数调用则调度器相当于每次从头开始执行，那么这样可能不适合于某些调度策略。并且swtch函数是汇编代码并不是平时的C代码函数，所以没有隐式的保存被调用者保存寄存器
    所以需要手动保存。并且要恢复上一次保存的调度器的状态。因为这个相当于普通的函数调用 所以保存的寄存器和普通的函数调用保存的相同就可以。每次在swtch函数ret时，直接返回到ra寄存器
    中保存的值。
    注意：每次调用函数时ra的值都会被更新

调度时遵循：
    遵循 “获取 - 切换 - 释放” 的顺序
    进程第一次执行的时候，会从frokret开始执行（释放锁） 
    注意：proc.c 中 allocproc 将第一次上下文切换的返回地址设置为forkret  p->context.ra = (uint64)forkret;