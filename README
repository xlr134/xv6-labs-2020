所以，从硬件和XV6的角度来说，当出现了pagefault，有3个极其有价值的信息，分别是：
    ·引起page fault的内存地址。
    ·引起page fault的原因类型。
    ·引起pagefault时的程序计数器值，这表明了pagefault在用户空间发生的位置。
    注意：引起page fault的内存地址。与引起pagefault时的程序计数器值，这表明了pagefault在用户空间发生的位置不相同，因为例如堆上的数据和指令肯定不在一个地址。

进程的虚拟地址空间（如 Linux 的虚拟内存布局）通常包含以下区域（从低地址到高地址）：
    代码段（Text Segment）
    数据段（Data Segment）
    堆（Heap）
    共享库映射区
    栈（Stack）
    未映射区域（理论上属于进程地址空间，但未分配物理内存）

    由进程地址空间映射知，可以通过 PGROUNDUP(p->trapframe->sp)-1<fault_va && fault_va<p->sz 来判断当前虚拟地址是否栈栈空间（trao.c 75）