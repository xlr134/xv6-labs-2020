xv6 常用汇编：
    csrrw rd, csr, rs1 // 交换寄存器 rd 和 csr 的值 csrrw是一个重要的特权指令，
    主要用于对处理器的控制状态寄存器（Control and Status Registers, CSRs） 进行读写操作
    rd：目标寄存器，用于存储从 CSR 读取的值。 csr：目标控制状态寄存器的地址或名称。rs1：源寄存器，其值将被写入 CSR。

    控制状态寄存器（Control and Status Registers, CSRs）是处理器中用于控制和监控处理器行为的特殊寄存器。
    在 RISC-V 架构（如 xv6 操作系统所基于的架构）中，CSRs 扮演着操作系统与硬件交互的桥梁角色，下面从功能、分类和典型应用三个方面详细介绍：

    sd rs2, offset(rs1)  RISC-V 汇编语言中的一条指令，用于 ** 存储双字（64 位数据）** 到内存。
    24:	4635                	li	a2,13 
    24:：指令的内存地址（十六进制），表示该指令存储在地址 0x24 处。
    4635：指令的机器码（十六进制），是指令的二进制编码形式。
    li a2,13：伪指令（Pseudo-instruction），等效于 addi a2, zero,  用于将立即数（常量）加载到寄存器中
    auipc（Add Upper Immediate to PC）：将 20 位立即数左移 12 位，加到当前 PC 上。
    addi a0,a0,1968  计算a0 = a0 + 1968
    jalr 1536(ra)   jalr（Jump and Link Register）：跳转到寄存器地址 + 偏移量，并保存返回地址到ra   计算目标地址：target = ra + 1536。


在进入用户空间之前，内核先设置`sscratch`指向该进程的`trapframe`，这个`trapframe`可以保存所有用户寄存器。
在（kernel/ trap.c:37）它保存了`sepc`（用户PC），这也是因为`usertrap`中可能会有一个进程切换，导致`sepc`被覆盖。如进行了时钟中断，可能会让出CPU，这时候
如果不保存pc那么中断没有办法返回到，异常发生出的地址。

uservec（trampoline页上）在改变`satp`后（存储页表）继续执行的原因，在用户页表和内核页表中，trampoline页被映射在相同的虚拟地址上。

ecall 会跳转到stvec指向的地址。

内核从trapframe中读取系统调用传递的参数

如果系统调用传递了指针类型的参数，指针会有以下两个问题：
    第一，用户程序可能是错误的或恶意的，可能会传递给内核一个无效的指针或一个旨在欺骗内核访问内核内存而不是用户内存的指针。
    第二，xv6内核页表映射与用户页表映射不一样，所以内核不能使用普通指令从用户提供的地址加载或存储。

为了解决这个问题，xv6中的copystr 将用户页表pagetable中的虚拟地址srcva复制到dst，需指定最大复制字节数。使用软件模拟分页硬件操作，确定srcva的物理地址，并且也能检测指针是否有效。

系统调用的时机：
    1. 系统调用
    2. 时钟/设备中断
    3. 异常
trap的处理流程：
    通常的顺序是：
        trap迫使控制权转移到内核；
        内核保存寄存器和其他状态，以便恢复执行；
        内核执行适当的处理程序代码（例如，系统调用实现或设备驱动程序）；
        内核恢复保存的状态，并从trap中返回；代码从原来的地方恢复执行。

用户空间的trap、内核空间的trap、定时器中断，三种不同的情况设置单独的汇编入口和C trap处理程序会更方便：
    stvec(Supervisor Trap-Vector Base Address Register):内核在这里写下trap处理程序的地址；RISC-V跳转到这里来处理trap;
    sepc(Supervisor Exception Program Counter):内核在这里写下trap返回的地址；RISC-V在trap处理程序返回时跳转到这里。
    scause(Supervisor Cause Register):内核在这里写下trap的原因；RISC-V在trap处理程序中检查这个寄存器来确定trap的原因。(监督模式原因寄存器)
    sscratch Supervisor Scratch Register（监督模式暂存寄存器）
    `sstatus`：`sstatus`中的**SIE**位控制设备中断是否被启用，如果内核清除**SIE**，RISC-V将推迟设备中断，直到内核设置**SIE**。**SPP**位表示trap是来自用户模式还是supervisor模式，并控制`sret`返回到什么模式。
    上述寄存器在用户模式下不能读写。
    用户空间的trap：处理路径 uservec(kernel/trampoline.S:16)->usertrap(kernel/trap.c:23)->syscall->usertrapret(kernel/trap.c:79)->usertrapret(kernel/trap.c)->userret(kernel/trampoline.S:32)
    
    因为RISC-V在trap过程中不切换页表，所以用户页表必须包含`uservec`的映射，即`stvec`指向的trap处理程序地址。`uservec`必须切换`satp`，使其指向内核页表；为了在切换后继续执行指令，
    `uservec`必须被映射到内核页表与用户页表相同的地址。（所以trampoline.S在内核态和用户态下的虚拟地址是一样的）
    
    trapframe 保存在用户地址空间中。因为在uservec时，satp仍然指向用户页表，所以要访问trapframe需要用户地址，所以trapframe保存在用户页表中（固定的TRAPFRAME 也就是`TRAMPOLINE`的下面）
    进程的`p->trapframe`也指向`trapframe`，不过是指向它的物理地址，这样内核可以通过内核页表来使用它。

    双重视角：物理地址 vs 虚拟地址
        虚拟地址：
        创建进程时，会进行地址映射：
        （p->trapframe = (struct trapframe *)kalloc() proc.c 111） // 返回物理地址
        if(mappages(pagetable, TRAPFRAME, PGSIZE,                 //  pagetable_t proc_pagetable(struct proc *p)   proc.c 进行地址映射
              (uint64)(p->trapframe), PTE_R | PTE_W) < 0){
                uvmunmap(pagetable, TRAMPOLINE, 1, 0);
                uvmfree(pagetable, 0);
                return 0;
            }
        并且在usertrapret时，会进行地址映射：
        uint64 fn = TRAMPOLINE + (userret - trampoline);
        ((void (*)(uint64,uint64))fn)(TRAPFRAME, satp);
        其中TRAPFRAME是tramframe 对应的虚拟地址。
        通过trampoline.S 中userret中的：
        # restore user a0, and save TRAPFRAME in sscratch  保存trapframe帧  
        csrrw a0, sscratch, a0  
    
    内核空间的trap:
        内核空间的trap 只处理两种情况：设备中断和异常。（无系统调用）
