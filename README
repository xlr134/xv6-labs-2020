实现了 trance系统调用：
    trance：在当前进程中跟踪是否调用了某个系统调用 并打印结果
实现了sysinfo系统调用：
    获取当前空闲内存和已创建进程的数量
扩展 trance：
    实现对各个系统调用所用参数的跟踪


待解决1：
    sysproc.c 中代码if(copyout(myproc()->pagetable,dstaddr,(char *)&info,sizeof info)<0) 为什么pagetable 指向用户空间的sysinfo 的结构体

待解决2：
    通过sysinfo计算平均负载，可能需要通过定时器中断来记录，每次调用时通过sysinfo返回平均负载
    平均负载解释
    平均负载（Load Average） 是衡量系统整体负载情况的关键指标，反映了系统在一段时间内处于 可运行状态（Runnable） 和 不可中断睡眠状态（Uninterruptible Sleep） 的进程数量。
    可运行状态进程
        指正在使用 CPU 或等待 CPU 资源的进程（即处于 R 状态的进程）。
    不可中断睡眠状态进程
        指因等待 I/O 操作（如磁盘、网络）而暂时无法运行的进程（即处于 D 状态的进程）。
    注意：这类进程不会被系统中断，直到 I/O 操作完成，因此会直接影响系统负载。
    平均负载的本质
        平均负载是 单位时间内上述两类进程的平均数量，数值越大表示系统负载越高。
    
    平均负载计算方式：
        平均负载通过 指数加权移动平均（Exponentially Weighted Moving Average，EWMA） 算法计算，公式为： Load(t)=α×Active Processes(t)+(1−α)×Load(t−1)
        α是衰减因子（与时间窗口相关），
        Active Processes(t) 是当前时刻的活动进程数（可运行 + 不可中断睡眠）。
    三个常用时间窗口（以 Linux 为例）：
        1 分钟  α = 1/60
        5 分钟  α = 1/300
        15分钟  α = 1/900

    与 CPU 核心数对比
        平均负载的合理范围需结合系统的 CPU 核心数 判断：
            若平均负载 ≤ CPU 核心数：系统负载正常，
            若平均负载 > CPU 核心数：可能存在性能瓶颈，需进一步分析。
            例：4 核 CPU 系统中，平均负载长期高于 4 则表明负载过高。
    数值波动的含义
        短期波动（1 分钟）：可能由突发任务（如编译代码、临时计算）引起，不一定代表系统故障。
        长期趋势（15 分钟）：若持续高于 CPU 核心数，需排查是否存在进程泄漏、死锁或资源竞争。

    与 CPU 利用率的区别：
        平均负载      可运行 + 不可中断睡眠的进程数平均值，包含 CPU 和 I/O 负载   系统整体压力
        CPU 利用率    CPU 处于繁忙状态的时间比例（仅衡量 CPU 自身的使用情况）     CPU 资源消耗
    
    典型场景：
        若平均负载高但 CPU 利用率低：可能是 I/O 瓶颈（如磁盘读写缓慢导致大量进程处于 D 状态）。
        若平均负载和 CPU 利用率均高：CPU 资源不足，需优化计算密集型任务。
    